{
  "name": "awaaz",
  "private": true,
  "engines": {
    "node": ">=20 <21",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev:api": "pnpm --filter @awaaz/api start:dev",
    "dev:api:no-redis": "pnpm --filter @awaaz/api start:dev:no-redis",
    "build:api": "pnpm --filter @awaaz/api build",
    "dev:web": "pnpm --filter web dev",
    "build:web": "pnpm --filter web build"
  },
  "devDependencies": {}
}
